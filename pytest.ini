[pytest]
markers =
    integration: marks tests as integration tests (require running Docker containers)
    unit: marks tests as unit tests (fast, no external dependencies)
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
